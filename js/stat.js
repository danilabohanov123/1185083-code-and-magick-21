"use strict"

const renderRect = (x, y, width, height, color = "#000000") => {
  ctx.fillStyle = color;
  ctx.fillRect(x, y, width, height);

};

window.renderStatistics = (ctx, names, times) => {
    
};